<html lang="pt-br">
<head>
    <%- include('partials/head') %>
</head>
<body class="home">
    <%- include('partials/header') %>
    <main>

        <h1 class="contraste"> Olá, Novato!</h1> <!-- ok -->
        <h2 class="contraste"> seja bem-vindo ao mundo de Mochileiro </h2>
        <% if (!usuario) { %>
            <p class="contraste"> Para começar a sua jornada, você precisa criar um cadastro ou fazer login. </p>
            <a class="contraste bold" href="/login"> Clique aqui para fazer login </a>
            <br></br>
            <a class="contraste bold" href="/cadastro"> Clique aqui para criar um cadastro </a>
        <% } else { %>
            <p class="contraste"> Você já está logado como <strong><%= usuario.nome %></strong>. </p>
            <a class="contraste bold" href="/saves"> Clique aqui para continuar sua aventura </a>
        <% } %>
    </main>
    <%- include('partials/footer') %>
</body>
<!-- 

*==========
 IDEIA TOP
*==========
<script>
    function gerar() {
        const r = Math.floor(Math.random() * 256);
        const g = Math.floor(Math.random() * 256);
        const b = Math.floor(Math.random() * 256);
        document.body.style.backgroundColor = `rgb(${r}, ${g}, ${b})`;
        const hsl = rgbToHsl(r, g, b);
        const hComplementar = (hsl.h + 180) % 360;
        const rgbComp = hslToRgb(hComplementar, hsl.s, hsl.l);

        const corComplementar = `rgb(${rgbComp.r}, ${rgbComp.g}, ${rgbComp.b})`;
        document.querySelectorAll('.contraste').forEach(el => {
            el.style.color = corComplementar;
        });
    }
    function rgbToHsl(r, g, b) {
        r /= 255; g /= 255; b /= 255;

        const max = Math.max(r, g, b);
        const min = Math.min(r, g, b);
        let h, s, l = (max + min) / 2;

        if (max === min) {
            h = s = 0;
        } else {
            const d = max - min;
            s = l > 0.5 ? d / (2 - max - min) : d / (max + min);

            switch (max) {
                case r: h = (g - b) / d + (g < b ? 6 : 0); break;
                case g: h = (b - r) / d + 2; break;
                case b: h = (r - g) / d + 4; break;
            }
            h *= 60;
        }

        return { h, s, l };
    }
    function hslToRgb(h, s, l) {
        const c = (1 - Math.abs(2 * l - 1)) * s;
        const x = c * (1 - Math.abs((h / 60) % 2 - 1));
        const m = l - c / 2;
        let r, g, b;

        if (h < 60)      [r, g, b] = [c, x, 0];
        else if (h < 120)[r, g, b] = [x, c, 0];
        else if (h < 180)[r, g, b] = [0, c, x];
        else if (h < 240)[r, g, b] = [0, x, c];
        else if (h < 300)[r, g, b] = [x, 0, c];
        else             [r, g, b] = [c, 0, x];

        return {
            r: Math.round((r + m) * 255),
            g: Math.round((g + m) * 255),
            b: Math.round((b + m) * 255)
        };
    }
    gerar();
    setInterval(() => {
        gerar();
    }, 2000);
</script>

-->
</html>